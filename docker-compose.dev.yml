services:
  postgres:
    ports:
      - '5432:5432'
    environment:
      POSTGRES_LOG_STATEMENT: all

  redis:
    ports:
      - '6379:6379'
    command: redis-server --loglevel verbose

  minio:
    ports:
      - '9000:9000'
      - '9001:9001'
    environment:
      MINIO_BROWSER: "on"
